version: '3'
services:
  api:
    image: "{{ api_docker_image }}:{{ api_docker_tag }}"
    environment:
      REDIS_HOST: "{{ api_redis_host }}"
      REDIS_PORT: "{{ api_redis_port }}"
    ports:
      - "{{ api_port }}:3000"
